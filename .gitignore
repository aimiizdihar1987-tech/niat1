# ============================================================
#  NIAT — .gitignore
#  This repo is PUBLIC. Everything listed here stays local only:
#  secrets, credentials, student PII, copyrighted reference material,
#  and generated/runtime data. See SECURITY.md.
# ============================================================

# ---- Secrets & credentials — never commit ----
apikey.txt
gamma_apikey.txt
gamma api key prestij.txt
auth_secret.txt
reminder_config.txt
supabase_config.txt
PROJECT_ID*.txt
users.json
client_secret.json
token.json
oauth_agent5_*
.env
.env.*
!.env.example
niat_hub.gs
niat_mail_webhook.gs

# ---- Student & personal PII ----
Email Student Prototype.txt
jadualwaktu*.pdf

# ---- Private folder: personal docs, reference PDFs, school data ----
# (dokumen-peribadi/, rujukan/, data-sekolah/ — never commit, never deploy)
private/

# ---- Copyrighted textbook PDFs: never commit, never deploy ----
textbooks/
.ipynb_checkpoints/

# ---- Real operational data (school-specific; keep local, not in a public repo) ----
classrooms.json
timetable.json
schools.json

# ---- Generated data & runtime artifacts ----
bank_soalan.db
output/
web/avatars/
announcement.json
*.log
watchdog_log.txt
reminders_log.txt
reminder_alert.json
__pycache__/
*.pyc

# ---- Local tooling (not needed to run the app) ----
.claude/
.playwright-mcp/

# ---- Unrelated side projects / scratch output living in this folder ----
# classroom-simulator/ is its own Node/Next.js repo (has its own .git) —
# never nest it into Niat's history. outputs/ is one-off generated scratch
# (dummy roster spreadsheets, previews), not app source.
classroom-simulator/
outputs/

# ---- OS / editor junk ----
desktop.ini
