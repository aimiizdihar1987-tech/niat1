# Niat — Python dependencies.
# The web server (server.py) and everything it imports use ONLY the Python
# standard library — nothing is required to deploy the app itself.
#
# The lines below are OPTIONAL local tools; uncomment only if you run them
# inside the same environment:
#
# fpdf2                     # generate_timetable_pdf.py (timetable PDFs)
# google-auth               # niat_google.py (Gmail/Google integration)
# google-auth-oauthlib      # niat_google.py
# google-api-python-client  # niat_google.py
